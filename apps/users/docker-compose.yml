services:
    app:
        container_name: tesseract-bot
        build:
            context: .
            dockerfile: ./Dockerfile
        image: tesseract-bot
        env_file: .env
        network_mode: host
        restart: always
        volumes:
            - /etc/ssl/certs/tg:/etc/ssl/certs/tg:ro
